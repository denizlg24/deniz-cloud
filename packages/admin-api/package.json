{
  "name": "@deniz-cloud/admin-api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "seed:admin": "bun run src/seed.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@deniz-cloud/shared": "workspace:*",
    "drizzle-orm": "^0.45.1",
    "hono": "^4",
    "meilisearch": "^0.55.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "mongodb": "^7.1.0",
    "postgres": "^3.4.8"
  }
}
